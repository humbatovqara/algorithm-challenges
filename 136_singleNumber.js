var singleNumber = function (nums) {
  if (nums.length === 0) {
    return false;
  }

  const singleNum = "";
  const count = 0;

  for (let i = 0; i < nums.length; i++) {
    console.log(nums.filter());
  }
};

singleNumber([2, 2, 1]);
